{{> layout/header}}

<div class="container p-5">

    <!-- 요청을 하면 localhost:8080/board/save POST로 요청됨
    title=사용자입력값&content=사용자값 -->
    <div class="card">
        <div class="card-header"><b>동영상 업로드</b></div>
        <div class="card-body">
            <form method="POST" enctype="multipart/form-data" action="/board/upload">
                <input type="file" name="file"/>
                <button type="submit">Upload</button>
            </form>
        </div>
    </div>
</div>

{{> layout/footer}}